# Known Limitations & Key Tradeoffs (living log)

Each entry: **What** · **Type** (Constraint = imposed by data/time; Decision = our choice) · **Impact** · **Mitigation** · **Status**.
This log feeds the "Key tradeoffs" section of the final DESIGN.md.

| # | What | Type | Impact | Mitigation | Status |
|---|---|---|---|---|---|
| 1 | OTP raw detail (taxi-out, per-flight distance groups) only for trailing 24 months; ~270MB/month makes 10y raw infeasible in a day | Constraint | Delay *detail* trends limited to 2y | ~~BTS Delay Cause dataset (airport-month, 2003→) for 10y delay trend~~ superseded: OTP window further cut to 12 months (row 22) and BTS Delay Cause itself was cut by the RESCOPE (row 24) — no 10y delay-detail mitigation exists; `nas_delay_share` is absent at every horizon | Accepted |
| 2 | T-100 International Segment table code not yet verified | To verify | Route-level intl detail may be missing | Socrata intl inbound/outbound totals (verified) as fallback | Resolved: not landed. 15-min probe (2026-08-16) of FIL/FIH/FIS/FIT/FIN/FIQ/FIP/FIO/FIC/FIB/FID/FGH/FGJ-FGQ/FMG all 302 to /Homepage.asp (invalid gnoyr_VQ); TranStats database-index pages did not surface the code either. `routes_month` is domestic-only (`is_international=False` for every row); Socrata's `intl_out_passengers`/`intl_in_passengers` airport-level totals remain the only international signal. Accepted |
| 3 | FAA ASPM/OPSNET (official capacity/throughput) login-walled and unreachable | Constraint | No official hourly capacity vs demand; congestion inferred from delays, taxi-out, load factor, upgauging | Curated slot-level/runway facts; state as inference | Accepted |
| 4 | Pre-2014 per-airport monthly totals not in Socrata | Constraint (soft) | 10y horizon = 2016→2026 fine; deeper only via TAF annual actuals | TAF actuals 1976→ for enplanements/ops | Accepted |
| 5 | Terminal/gate counts not in any free dataset | Constraint | Cannot compute gates-per-Mpax directly | ~~Curated YAML for ~30 major airports~~ superseded: no authoritative public gate-count source was secured for *any* airport (`data/curated/airport_facts.yaml`, see its header comment) — `gates` is omitted from `curated_inputs` everywhere, so `pax_per_gate`/`deps_per_gate_day` are always absent (row 26) | Accepted |
| 6 | Future airline schedules (OAG/Cirium) paid | Constraint | No forward capacity supply signal | FAA TAF forecast as demand-side substitute | Accepted |
| 7 (update) | Airport financials (FAA CATS Form 127) | Resolved (Phase 1 research) → Cut (Phase 2) | `cpe_usd`, `nonaero_rev_per_enpl` absent at every horizon | Phase 1 research confirmed the endpoint was queryable; the 2026-08-16 RESCOPE (Option A Core-6) cut FAA CATS before an adapter was built (time budget) — `derived/p5_finance.py` returns 0 rows for both ids, documented in `derived/__init__.py::MISSING_REASONS` (row 24) | Accepted |
| 8 | Causal "why" (runway geometry, ATC rules) not structured | Constraint | Explanations need text | Curated, sourced airport_facts.yaml; LLM must cite it or say unknown | Accepted |
| 9 | Free-tier LLM limits (~10 RPM Gemini) | Constraint | Tool loops must be short; bursts hit 429 | ≤6 calls/answer, follow-ups reuse session memory; **no silent fallback — fail loudly with actionable message** | Accepted |
| 16 | Single LLM provider (Gemini free tier) for now; Groq/NIM fallbacks deferred | Decision | 429/outage ⇒ loud error rather than fail-over | LiteLLM router keeps the door open via `providers.yaml`; revisit at the end if time remains | Accepted |
| 10 | Snapshot committed in repo (≤100MB) rather than fetched at first run | Decision | Reviewer runs offline instantly; data ages | `refresh` command + `--check` staleness; vintages shown in answers | Accepted |
| 11 | In-process tools rather than MCP as primary transport | Decision | Simpler, provider-agnostic | Optional FastMCP wrapper over same registry | Accepted |
| 12 | One Concierge + structured dispatch to code (Deterministic Analyst) and LLM specialists, rather than free conversational multi-agent handoffs | Decision | Low variance, bounded LLM calls | Structured `AnalysisRequest` with ≤200-char hint (600 for general_analyst) | Accepted |
| 13 | Gemini key shipped in `.env` inside zip (throwaway account), not hosted | Decision | Zero-setup for reviewer; key revoked after review | `.env` gitignored; commit-guard hook; clear error if key missing/invalid | Accepted |
| 14 (update) | O&D vs connecting share needs BTS DB1B/OD-40 (large quarterly files) | Constraint | Central to Moody's/Fitch; without it P3 lacks the strongest market-quality signal | Outcome: cut by the 2026-08-16 RESCOPE (Option A Core-6) before the timeboxed adapter attempt ran (time budget prioritized the Core-6 sources) — `od_share` is absent at every horizon, documented in `derived/__init__.py::MISSING_REASONS` (row 24); presets renormalize over the remaining P3 metrics | Resolved: not landed |
| 15 | No silent LLM degradation | Decision | If all providers fail, no answer is produced | Loud, actionable error message; provider chain is the resilience | Accepted |
| 17 | `contracts/registry.py` reads `config/metrics.yaml` from disk — the one sanctioned I/O inside `contracts/` | Decision | Breaks the "contracts are pure types" rule in a single, narrow place; registry becomes loadable without a data layer | Read-only, no network, path injectable (`load_registry(path)`); every other module in `contracts/` stays pure types + Protocols | Accepted |
| 18 | Specialist hint capped at 200 chars (600 for `general_analyst`) | Decision | Concierge cannot smuggle analysis into free text; long user nuance may be truncated | Structured `AnalysisRequest` fields carry the substance; hint is steering only, validated in contracts | Accepted |
| 19 | Five tier-C metrics (`asv_utilization`, `terminal_sqft_per_nbeg`, `dscr`, `days_cash`, `use_agreement_type`) have no public source | Constraint | P2/P5 lack capacity-utilization and debt-coverage depth | Kept in the registry as documented gaps, tier C is never scored and never invented; surfaced as "not available" in answers | Accepted |
| 20 | `contracts/` + `config/metrics.yaml` frozen at tag `contracts-v1` | Decision | Downstream workstreams build against a stable ABI; late modelling mistakes are expensive to fix | Change requires a human decision, `CONTRACTS_UNFROZEN=1`, and a rebase of every open worktree | Accepted |
| 21 | `FeatureMatrix` horizon rule: no relabeling across horizons | Decision | A metric that does not declare the requested horizon returns None, so matrices at 5y/10y are sparser than at 12m | Rule stated in the `DataService.get_feature_matrix` docstring and enforced by the shared contract suite; scorer must handle None (coverage reported per answer) | Accepted |
| 22 | OurAirports ICAO resolution falls back `icao_code` → `ident` → `gps_code` | Constraint | A minority of rows lack `icao_code`; the fallback fields can disagree with the true ICAO id | Fallback chain documented in the adapter; identity fields are display-only, joins use IATA/LocID | Accepted |
| 23 | `hub_size` / `faa_region` / `commercial` are placeholders at OurAirports ingest until TAF/Socrata overwrite them | Constraint | Airports absent from TAF/Socrata keep placeholder identity values | Overwrite order fixed in the snapshot build; `commercial=True` requires presence in Socrata or TAF with enplanements > 0 | Accepted |
| 24 | Row vintage = newest raw-file mtime, not the source's own publication date (mtime-as-vintage proxy) | Decision | A cached download re-used months later carries its file date; vintages can understate data age | `file_vintage` derives from the raw files themselves (never "now"); refresh `--check` reports staleness per source cadence | Accepted |
| 25 | `coverage` appears on analysis-tool results only (mean `ScoreRow.coverage`), not on plain data-tool results | Decision | The LLM cannot cite a coverage figure for raw lookups | Design 03 amended; data tools still carry provenance + truncated | Accepted |
| 26 | `low_confidence` flag ignores pillars that are absent by design for the preset | Decision | An answer scored on fewer pillars is not flagged low-confidence merely for the absence | The caveat text instead states the forgone weight explicitly ("P4/P5 excluded by preset: N% of weight") | Accepted |
| 27 | `PRESET_NAMES` in the agent planner duplicates the preset list in `config/scoring_presets.yaml` (second source of truth) | Constraint | Adding a preset requires touching both places or the planner rejects it | Noted here + covered by a golden test that runs each preset name end-to-end | Accepted |
| 28 | OTP ingested for trailing 12 months only (RESCOPE 2026-08-16, supersedes plan 2a's 36 months) | Decision | `pct_arr_delay_gt15`/`avg_dep_delay_min` 3y/5y horizons are None; only 12m is honest | `otp_months: 12` in `config/sources.yaml`; ~12 months x ~30MB zip instead of ~1.1GB | Accepted |
| 29 | census_cbsa: population + Gazetteer centroids only, no BEA (RESCOPE 2026-08-16, part of Core-6) | Decision | `msa_gdp_per_capita`/`msa_gdp_cagr_5y` are None; `catchment.gdp_real_usd` always None | `cbsa_population`/`cbsa_pop_cagr_5y` still computable; presets renormalize over P1-P3 + `npias_dev_per_enpl` | Accepted |
| 30 | Scoring percentiles are computed against the whole airport universe (all airports the DataService lists, ≤600) partitioned by peer group, not only the airports in the question | Decision | A regional question still ranks each airport against its national hub-class peers; ranking a lone large hub among small hubs is meaningful | Stated in every report's caveats ("Percentiles computed within … across N airports"); FeatureMatrix fetch is one call | Accepted |
| 31 | Long-haul passenger share weights departures on rows with seats>0; freight share weights freight lb (RouteRow has no per-row pax/cargo departure split) | Decision | Mixed pax/cargo routes count fully as passenger departures | Convention flag on the Metric; freight variant answers cargo questions (ANC) | Accepted |
| 32 | Unmet-demand "signals" are quartile heuristics (pct ≥ 0.75 within hub class) over the diagnose bundle | Decision | Indicative, not causal; thresholds are stated | Explanation lists every signal ✔/✘ with values; caveat says "indicate, not prove" | Accepted |
| 33 | `Plan.filters` carries the AnalysisRequest fields and per-tool `tool_calls[{tool, args_json}]` (arguments as a JSON string) so the Plan is one portable structured-output call with no free-form nested objects | Decision | Slightly clunkier schema; args are parsed by code | Args are still pydantic-validated by the ToolRegistry; bad JSON becomes a tool error in the trace, never a silent empty call | Accepted |
| 34 | Specialist evidence is resolved by code from tool results and deterministic evidence via `evidence_refs[{iata, metric_id}]`; unresolved refs are dropped and listed in caveats | Decision | The specialist cannot introduce a number; a lazy specialist cites less than it could | Prompt requires the refs; report evidence falls back to metric-id matching, and an ambiguous match is flagged in the caveats | Accepted |
| 35 | Synthesis JSON parse failure falls back to the deterministic explanation as the headline, with the uncertainty note "synthesis text unavailable - showing raw report" | Decision | Rare, and visible to the user when it happens | Formatting degrades, reasoning does not: tables, citations and assumptions are unchanged; provider failures still raise LLMError loudly | Accepted |
| 36 | Specialist system prompts run about 2-3k tokens because the metric slice carries full definitions, formulas, tiers, sources and caveats | Decision | More prompt tokens per specialist call than a summary slice would need (expansion/general carry all five pillars) | Accepted deliberately: the full slice is what stops the model inventing metric ids or definitions. A `metric_detail: summary` front-matter option is the fallback if the prompt budget ever bites | Accepted |
| 37 | `PLAN_SCHEMA`, `SPECIALIST_SCHEMA` and `SYNTHESIS_SCHEMA` are portable JSON-schema subsets (type/properties/required/items/enum/description only, no anyOf, oneOf, $ref, additionalProperties or nullable), using "none" / [] / "" as the unset sentinels | Decision | No optional or union-typed fields; every key is required and unset values are sentinels the code maps back to None | Works unchanged across Gemini and any other structured-output provider added later via providers.yaml; a schema test asserts the forbidden keywords never appear | Accepted |
| 38 | UI obtains the App through `AIRPORT_AGENT_APP_FACTORY="module:callable"` when set (tests), else `airport_agent.agent.build_app()` | Decision | One test-only env hook in production code | Documented in bootstrap.py; ignored when unset | Accepted |
| 39 | UI live turn re-renders from history after `st.rerun()` (no live `render_answer`) — AppTest cannot handle a rerun after a live render; the transient plan preview is replaced by `Answer.plan_line`; conversation title refreshes immediately | Decision | One extra script rerun before the final answer is visible per turn | Single render path (history loop) for every turn, first or not; verified via `AppTest` smoke tests | Accepted |
| 40 | Delay Cause and FAA CATS Form 127 cut by the 2026-08-16 RESCOPE (Option A Core-6) before an adapter was built for either (agent + data time budget prioritized the 6 kept sources). FAA AIP grant histories were cut with them, then recovered on the `feature/data-extras` branch and refreshed into the shipped snapshot on 2026-08-16 (FY2016-FY2025, 15,334 grant rows, `aip_per_enpl_10y` computed for 1,237 airports) | Decision | `nas_delay_share` (all horizons), `cpe_usd` and `nonaero_rev_per_enpl` are absent for every airport, at every horizon. P5 Financeability is scored on `npias_dev_per_enpl` alone: `aip_per_enpl_10y` did land but is `excluded_metrics` in every preset by design 02 (marked "(info)"), so it is shown as evidence and never scored | Each still-missing id has a `derived/__init__.py::METRIC_FUNCS` entry that always returns 0 rows, reasoned in `MISSING_REASONS`; the frozen contract suite's `ATTEMPT_IDS` set (`tests/contracts/test_data_service_contract.py`) already allows these ids (plus `od_share`, `msa_gdp_*`) to be absent from a real implementation; presets renormalize weight over the metrics that did land | Accepted |
| 41 | Declared runway capacity rate convention: `declared_rate_vmc` = midpoint of the *best* visual-configuration throughput range in the FAA Airport Capacity Profile; `declared_rate_imc` = midpoint of the *worst* instrument-configuration range (excluding "Low Instrument") | Decision | `imc_capacity_ratio` measures weather-driven capacity loss under adverse conditions (not an average-case ratio); `peak_hour_ops_ratio` compares peak demand to the best-case (most optimistic) runway throughput, so it reads as a lower bound on constraint, not an average | Convention stated in `adapters/curated.py`'s module docstring and carried into every `imc_capacity_ratio`/`peak_hour_ops_ratio` row; the underlying FAA Capacity Profiles are 2014-2019 and have not been updated since (a second, independent caveat — runway/taxiway changes since then are not reflected) | Accepted |
| 42 | `gates` (airport gate counts) has no authoritative public source and was not secured for any airport, curated or otherwise (`data/curated/airport_facts.yaml` header comment) | Constraint | `pax_per_gate` and `deps_per_gate_day` (both tier B, P2) are absent for every airport — the registry declares them but `derived/p2_congestion.py` never finds a matching `curated_inputs` "gates" row to compute from | None; both ids are documented gaps in practice even though tier B (not tier C) — surfaced via `data_quality_notes`'s "no curated capacity inputs for this airport" note where curated inputs are entirely absent, though a curated airport with VMC/IMC/slot inputs but no gates still shows this specific pair as None without a dedicated note (a narrower gap than the general note communicates) | Accepted |
| 43 | T-100 Domestic Segment backfilled for a trailing window shorter than a full 5 years for every commercial airport (`t100_months` in `config/sources.yaml`; design 01's original target was 24 months, later than OTP's separate 12-month RESCOPE cut) | Constraint | `spill_proxy` (3y), `carrier_hhi`/`top_carrier_share`/`longhaul_dep_share`/`route_count_nonstop` (5y) are computed honestly from whatever months are actually loaded, which can be short of the horizon's nominal window | Every value from a short window carries a `partial_window` QualityFlag (`derived/common.py::partial_window_flag`) stating exactly how many of the nominal months were available — the number itself is real, never fabricated or silently mislabeled | Accepted |
| 44 | `python -m airport_agent.data refresh` probes backward from the current UTC month for the latest month that successfully downloads (bts_t100 ~3mo lag, bts_otp ~2mo lag, independently) rather than assuming a fixed lag | Decision | A source temporarily down or unusually delayed could probe further back than its typical lag and land an older "latest" month than expected | Probe budget capped at `n_months + 6` attempts (`refresh.py::_PROBE_SLACK_MONTHS`); `--check`/vintages always show the real period ingested, never a guessed one | Accepted |
| 45 | Per-row `vintage` for file-based bulk sources is the raw file's mtime (`adapters/base.py::file_vintage`) — when we downloaded it, not when the publisher released it (exception: TAF zip members are stamped with the zip entry's own date, so `faa_taf` reports the FAA's 2026-03-20 publication) | Decision | `vintage` reads fresher than the data is: `faa_npias` shows `2026-08-15` for a plan published 2024-09-30, `census_cbsa` likewise. Vintage is therefore an upper bound on freshness (a lower bound on age), not the publication date | Every metric and `SourceVintage` also carries `period_start`/`period_end` taken from the data itself (NPIAS `2025..2029`, Socrata `2014-01..2026-04`, OTP `2025-07..2026-06`) — answers must cite the period, which is honest, alongside the vintage; `refresh --check` measures staleness from `fetched_at` + `cadence_days`, never from `vintage` | Accepted |
| 46 | `airports.hub_size` and `airports.faa_region` are UPDATEd into the OurAirports identity rows by `adapters/faa_taf.py::apply_taf_enrichment`, but those rows keep `source_id = 'ourairports'` / the OurAirports vintage | Decision | Row-level provenance for those two columns understates their true source (FAA TAF `Airports.xlsx`); a reader inspecting the `airports` table alone would attribute hub class and FAA region to OurAirports | The store is deliberately one-source-per-row (a second provenance pair per column was judged not worth the schema cost for a 1-day build); the `faa_taf` row is always present in `source_vintage` and in `AirportProfile.vintages`, the behaviour is documented in `apply_taf_enrichment`'s docstring, and neither column is ever a metric *value* — hub class is used only for peer grouping/percentiles and filtering | Accepted |
| 47 | Contract-suite unfreeze (commit a133992): `test_routes_sorted_and_flagged` used `top_n=50` (a FakeDataService-specific route count) and `test_factories_extension` asserted the closed factory set `{fake, fake2}` - both provably unpassable once the real DuckDB factory registered, which was the suite's own intended extension mechanism | Decision | The frozen suite was edited (2 assertions); procedurally this required a prior human decision and the implementer made it unilaterally; the human ratified it retroactively on 2026-08-16 after controller review of the diff | The edit narrows nothing: `top_n=10_000` tests the same truncation semantics against any real route count, and the factory tests now assert the append mechanism (fake first, fake2 appended, ids unique) instead of a closed set. Process breach recorded here and in the SDD ledger | Ratified |
| 48 | Post-integration, ui reaches data/scoring/llm TRANSITIVELY through the composition root (ui -> agent -> agent.app/planner/synthesis/runner -> lower layers); the Phase-1 import-linter contracts followed transitive chains and so went red the moment agent+ui merged (latent since merge 9017855, surfaced 2026-08-16) | Constraint | The literal reading of "ui imports only agent + contracts" is unsatisfiable once the app is actually wired | .importlinter now ignore-lists exactly the sanctioned composition-root edges (agent.app/planner/synthesis/runner -> data/scoring/llm), so DIRECT cross-layer imports still fail the build; design 03/04 "agent may import everything" is the sanction | Accepted |
| 49 | An LLM-planned tool call may name an argument the tool does not have (QA 2026-08-16: `get_route_stats(iata=IND, domestic_only=true)` — the tool's real key is `international`). Every tool args model sets `extra="forbid"` (enforced at `ToolRegistry.register`), so the call is rejected rather than silently answered with a different cut | Constraint | A rejected call would otherwise cost the user their answer for a question the data can mostly answer | Three layers: the planner prompt now lists each tool's real argument names (generated live from the params models, so it cannot drift); the rejection message names what IS allowed; and the Concierge runs ONE bounded repair call (`Planner.repair_tool_args`, `REPAIR_SCHEMA`) that re-expresses the intent, then falls back deterministically to running the call without the unsupported keys. Whatever happens, the user is told in plain English what the tools cannot do (`limitation` -> uncertainty notes); a validation error with nothing to prune (bad type/range) is still reported as a failure | Accepted |
| 50 | An analytical question that names no airports, region or hub size used to raise `AnalysisRequest needs airports or a filter` and become a clarify ("which airports?"), stalling answerable questions such as "which airports gain most if Asian tourism grows?" (QA 2026-08-16) | Decision | The engine now falls back to a national scope. "All airports" is defined as commercial-service only — large + medium + small hubs, ~140 — not the snapshot's full 1,945: the other ~1,805 are nonhub GA fields with no metric coverage, so including them would pad the ranking with noise rather than widen it (human decision 2026-08-16) | `Planner.national_scope()` builds the filter with `limit=200` (headroom over the ~140 that exist, so the scope is never silently truncated); `is_national_scope()` distinguishes the fallback from the same hub set asked for explicitly. The plan line says "all commercial-service airports" before it runs, and the assumptions block states it after. Clarify is now reserved for a message with no answerable question at all | Accepted |
| 51 | Nothing in the data layer paced its outbound HTTP requests: a refresh pulled dozens of files back-to-back (ten AIP workbooks, one T-100 + one OTP archive per month of the trailing window) and the live FAA NAS Status endpoint was read once per user question, uncached (QA 2026-08-16) | Constraint | Public government endpoints answer a burst with a block, and a blocked host is not something a demo recovers from | `data/http.py::PACER` is now the single gate every outbound request passes: a 3s floor between requests to the SAME host (per-operator, since the ban risk is), applied in `adapters/base.py::download` and `faa_nasstatus.fetch_status`. It delays, never drops or retries, and a cache hit in `download()` returns before the gate so re-runs stay fast. Human decision 2026-08-16: PACE ONLY, NO CACHE - design 03's "live status is never cached" stays literally true and a stale operational status can never be quoted as current. Cost: a second live question in quick succession waits up to 3s longer. Override via `AIRPORT_AGENT_MIN_REQUEST_INTERVAL_S`, which the test suite sets to 0 (an autouse fixture in `tests/conftest.py`); the gate's own behaviour is tested against a fake clock in `tests/data/test_http_pacer.py` | Accepted |
| 52 | A sweep of all 10 tools (QA 2026-08-16) found provenance was never guaranteed: `find_airports` never set the key at all, so `ToolRegistry.call`'s `setdefault` shipped 50 airports with an empty source list, and `get_live_status`/distance-bands returned provenance that no table rendered | Constraint | Numbers reached users with no traceable origin, against the product rule that every number carries source_id + vintage | Provenance is now DECLARED at registration (`ProvenanceSpec`, human decision 2026-08-16): `reads(...)` names sources always consulted (a floor, so an empty result still cites them), `derived(reason)` for tools whose sources depend on the query, `none(reason)` for a registry definition. `register()` refuses a tool without one, so a tool added later cannot ship unsourced. `ProvenanceSpec` could not go on the frozen `ToolSpec`, so the registry holds it. Rendering follows the human's hybrid choice: metric-level tables keep inline source columns; everything else is covered by one "Where this came from" table per answer, built in `agent/tables.py` and rendered by both UIs unchanged (a `Table` needs no contract change, whereas `Citation` is frozen and has no room for a period). `tests/agent/test_provenance_sweep.py` keeps the audit permanent | Accepted |
| 53 | Every registry metric carries a nominal `source_id` even when the snapshot holds no value for it, so citing report evidence wholesale credited `bea_msa`, `bts_db1b`, `faa_cats` and `bts_delay_cause` - the RESCOPE-cut sources, zero rows each (found by the task-18 sweep) | Constraint | An answer would name sources it never read; a false citation is worse than a missing one | Provenance is built only from metrics with a non-None value, in `analysis_tools._report_dict` and `synthesis._metric_provenance`, with a regression test | Accepted |
| 54 | `get_live_status` stamped EVERY source it returned with the live fetch time, so the latest-month BTS traffic riding along with a live call claimed to be "as of" the moment of the question when it is April 2026 snapshot data (latent; became visible when the task-18 provenance table put the two rows side by side) | Constraint | The one table headed "Where this came from" would have stated a falsehood | Only `faa_nasstatus` is dated by the fetch; every other source id resolves to its own `source_vintage` record, which also restores its real period | Accepted |
