# secrets
.env
*.key
# python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
# data
data/raw/
data/cache/
data/sessions/

# Dev-time debug log (runtime-debug-log design 2026-08-16): structured JSONL per session,
# mirrors the raw errors/coverage the user surface no longer shows. Never in git or the zip.
data/debug/
# os/ide
.DS_Store
Thumbs.db
.idea/
.vscode/
.superpowers/

# Scribe output, re-created on any agent run: the raw feed by .claude/hooks/log_agent_stop.py,
# the narrative by the process-scribe. Dev-time artifacts, not deliverables -- docs/WHERE-HOW-AI-IS-USED.md
# is the shipped account (known-limitations rows 61-62). Also in make_zip.py's EXCLUDE_NAMES,
# because docs/ ships by rglob and a regenerated file would otherwise slip back into the zip.
docs/process-log.raw.jsonl
docs/process-log.md
